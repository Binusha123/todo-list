# todo-list
A simple and efficient Todo List application built with Node.js. This project demonstrates CRUD (Create, Read, Update, Delete) operations, allowing users to manage tasks seamlessly. It uses Express.js for routing, SQL  for persistence, and follows a clean, modular structure for scalability.
